import 'package:booqs_mobile/models/quiz_request.dart';
import 'package:flutter/material.dart';

class QuizRequestItemPendingReason extends StatelessWidget {
  const QuizRequestItemPendingReason({super.key, required this.quizRequest});
  final QuizRequest quizRequest;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
